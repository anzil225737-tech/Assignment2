# PeopleOS Employee Dashboard

A React employee dashboard with a read-only JSONPlaceholder directory and a local employee details form.

## Run locally

```bash
npm install
npm run dev
```

The dashboard reads employee `id`, `name`, and `email` from `https://jsonplaceholder.typicode.com/users`. Form submissions are handled locally and are not posted to an API.

